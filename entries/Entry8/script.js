function toggleParagraph(paragraphId) {
            var paragraph = document.getElementById(paragraphId);
            paragraph.style.display = (paragraph.style.display === "none" || paragraph.style.display === "") ? "block" : "none";
        }

        function showImage1(imageSrc, hiddenImageId) {
            var hiddenImage = document.getElementById(hiddenImageId);
            hiddenImage.src = imageSrc;
            hiddenImage.style.display = 'block';
        }

        function hideImage1(hiddenImageId) {
            var hiddenImage = document.getElementById(hiddenImageId);
            hiddenImage.style.display = 'none';
        }

        function showImage2(imageSrc, hiddenImageId) {
            var hiddenImage = document.getElementById(hiddenImageId);
            hiddenImage.src = imageSrc;
            hiddenImage.style.display = 'block';
        }

        function hideImage2(hiddenImageId) {
            var hiddenImage = document.getElementById(hiddenImageId);
            hiddenImage.style.display = 'none';
        }

        function showImage3(imageSrc, hiddenImageId) {
            var hiddenImage = document.getElementById(hiddenImageId);
            hiddenImage.src = imageSrc;
            hiddenImage.style.display = 'block';
        }

        function hideImage3(hiddenImageId) {
            var hiddenImage = document.getElementById(hiddenImageId);
            hiddenImage.style.display = 'none';
        }

        function showImage4(imageSrc, hiddenImageId) {
            var hiddenImage = document.getElementById(hiddenImageId);
            hiddenImage.src = imageSrc;
            hiddenImage.style.display = 'block';
        }

        function hideImage4(hiddenImageId) {
            var hiddenImage = document.getElementById(hiddenImageId);
            hiddenImage.style.display = 'none';
        }

        function showImage5(imageSrc, hiddenImageId) {
            var hiddenImage = document.getElementById(hiddenImageId);
            hiddenImage.src = imageSrc;
            hiddenImage.style.display = 'block';
        }

        function hideImage5(hiddenImageId) {
            var hiddenImage = document.getElementById(hiddenImageId);
            hiddenImage.style.display = 'none';
        }